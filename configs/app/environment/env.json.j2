{
    "containerOverrides": [
        {
            "name": "aws-load-testing",
            "environment": [
                {
                    "name": "APPLICATION",
                    "value": "{{ env.TARGET_APP }}"
                },
                {
                    "name": "ENVIRONMENT",
                    "value": "{{ env.TARGET_ENV }}"
                }
            ]
        }
    ]
}
